<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        Dayily Enerji - Lityum Enerji Depolama Sistemleri, Perakende Pil ve Batarya Malzemeleri 🔋
    </title>
    <link rel="icon" href="/img/logo_mini.png" type="jpeg">
    <link href="~/css/global.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet" />
    <link href="~/css/output.css" rel="stylesheet" />

    <!-- Alpine -->
    <script nonce="@Csp.ScriptNonce" defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <style nonce="@Csp.StyleNonce">
        [x-cloak] {
            display: none !important
        }</style>
    <script nonce="@Csp.ScriptNonce">
        document.addEventListener('alpine:init', () => {
          Alpine.data('passwordField', () => ({
            show: false,
            toggle(){ this.show = !this.show },
            hold(){ this.show = true },
            release(){ this.show = false }
          }));
        });
    </script>
</head>

<body class="min-h-screen bg-gray-50 relative">
    <!-- Top-left return -->
    <a href="/Home/Index"
       class="fixed top-5 left-5 sm:top-6 sm:left-6 z-10
            group inline-flex items-center gap-1.5
            text-base sm:text-lg font-semibold
            text-gray-800 hover:text-amber-500
            px-3 py-2 rounded-xl bg-white/70 backdrop-blur-md
            ring-1 ring-black/5 shadow transition">
        <svg class="h-5 w-5 transition-transform group-hover:-translate-x-1"
             viewBox="0 0 24 24" fill="none" aria-hidden="true"
             xmlns="http://www.w3.org/2000/svg">
            <path d="M19 12H5M11 5l-7 7 7 7"
                  stroke="currentColor" stroke-width="2"
                  stroke-linecap="round" stroke-linejoin="round" />
        </svg>
        <span>Ana sayfa</span>
    </a>

    <div class="grid min-h-screen place-items-center px-4">
        <div class="w-full max-w-md animate-fadeIn">
            <div class="rounded-2xl bg-white/70 backdrop-blur-md p-8
                  shadow-[0_12px_32px_rgba(0,0,0,0.08)] ring-1 ring-black/5">
                <div class="mb-8 flex justify-center">
                    <a href="/Home/Index">
                        <img src="/img/logo_gray.png" alt="Logo" class="mx-auto h-24" />
                    </a>
                </div>
                @RenderBody()
            </div>
        </div>
    </div>

    <script nonce="@Csp.ScriptNonce" src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
</body>
</html>

